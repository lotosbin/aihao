product: jvm/app

dependencies:
  - //core
  - $ktor.server.core
  - $ktor.server.netty
  - $libs.logback.classic

settings:
  ktor: enabled
  jvm:
    mainClass: com.yuanjingtech.aihao.server.ApplicationKt

test-dependencies:
  - $ktor.server.testHost
